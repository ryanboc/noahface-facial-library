<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><title>Roster {{ $weekStart->format('d M Y') }}</title><style>
body{font-family:Arial,sans-serif;color:#111;margin:28px}header{display:flex;justify-content:space-between;align-items:end;border-bottom:2px solid #111;padding-bottom:12px;margin-bottom:18px}h1{margin:0;font-size:24px}.muted{color:#555;font-size:12px}table{width:100%;border-collapse:collapse;font-size:12px}th,td{border:1px solid #999;padding:8px;text-align:left;vertical-align:top}th{background:#eee}.day{font-weight:bold;width:13%}.shift{margin-bottom:7px}.shift:last-child{margin:0}.name{font-weight:bold}.empty{color:#888}@page{size:landscape;margin:12mm}@media print{body{margin:0}.no-print{display:none}}
</style></head><body><header><div><div class="muted">NOAHFACE SYNC · MANAGER ROSTER</div><h1>Weekly roster</h1><div>{{ $weekStart->format('Monday, d M') }} – {{ $weekEnd->format('Sunday, d M Y') }}</div></div><button class="no-print" onclick="window.print()">Print</button></header>
<table><thead><tr>@foreach($days as $day)<th>{{ $day->format('D') }}<br>{{ $day->format('d M') }}</th>@endforeach</tr></thead><tbody><tr>@foreach($days as $day)<td>@forelse($shifts->filter(fn($shift) => $shift->shift_date->isSameDay($day)) as $shift)<div class="shift"><div class="name">{{ $shift->employee->name }}</div><div>{{ \Carbon\Carbon::parse($shift->start_time)->format('g:i A') }} – {{ \Carbon\Carbon::parse($shift->end_time)->format('g:i A') }}</div>@if($shift->role || $shift->location)<div class="muted">{{ collect([$shift->role, $shift->location])->filter()->join(' · ') }}</div>@endif</div>@empty<span class="empty">No shifts</span>@endforelse</td>@endforeach</tr></tbody></table><p class="muted">Printed {{ now()->format('d M Y, g:i A') }}</p><script>if(new URLSearchParams(location.search).has('autoprint'))window.print()</script></body></html>
